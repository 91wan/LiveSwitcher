import Foundation

/// LiveSwitcher application constants.
enum AppConfiguration {
    /// 应用显示名称
    static let appName = "LiveSwitcher"
    /// 应用版本
    static let appVersion = "0.3.2"
    /// 统一 Bundle Identifier
    static let bundleIdentifier = "com.91wan.liveswitcher"
    /// 窗口最小宽度
    static let minWindowWidth: CGFloat = 1360
    /// 窗口最小高度
    static let minWindowHeight: CGFloat = 700
    /// 主监视器背景色（Program Monitor）
    static let monitorBackgroundColor = "000000"
}
